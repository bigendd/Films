{% extends 'base.html.twig' %}

{% block title %}Favoris{% endblock %}

{% block body %}
    <h1>Favoris</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ favori.id }}</td>
            </tr>
            <tr>
                <th>FilmId</th>
                <td>{{ favori.filmId }}</td>
            </tr>
            <tr>
                <th>DateDeCreation</th>
                <td>{{ favori.dateDeCreation ? favori.dateDeCreation|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ favori.statut ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_favoris_index') }}">back to list</a>

    <a href="{{ path('app_favoris_edit', {'id': favori.id}) }}">edit</a>

    {{ include('favoris/_delete_form.html.twig') }}
{% endblock %}
