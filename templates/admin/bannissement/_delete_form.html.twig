<form method="post" action="{{ path('admin_bannissement_delete', {'id': bannissement.id}) }}" onsubmit="return confirm('T'es sur de supprimer le bannissement?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bannissement.id) }}">
    <button class="btn">Delete</button>
</form>
