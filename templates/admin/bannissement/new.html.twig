{% extends 'base.html.twig' %}

{% block title %}New Bannissement{% endblock %}

{% block body %}
    <h1>Create new Bannissement</h1>

    {{ form_start(form) }}
        {{ form_row(form.raison) }}
        {{ form_row(form.utilisateur) }}
        {{ form_row(form.duree) }}
        <button class="btn">{{ button_label|default('Save') }}</button>
    {{ form_end(form) }}

    <a href="{{ path('admin_bannissement_index') }}">back to list</a>
{% endblock %}
