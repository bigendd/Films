{% extends 'base.html.twig' %}

{% block title %}Contact Us{% endblock %}

{% block body %}
    <h1>Contact Us</h1>

    {{ form_start(form) }}
        {{ form_row(form.email) }}
        {{ form_row(form.objet) }}
        {{ form_row(form.corps) }}
        <button class="btn btn-primary">Send</button>
    {{ form_end(form) }}
{% endblock %}
